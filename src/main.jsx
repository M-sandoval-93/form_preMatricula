import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import FormPreMatricula from './component/FormEstudiante'
import PreMatricula from './component/PreMatricula'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PreMatricula />
  </React.StrictMode>,
)
